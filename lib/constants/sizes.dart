import 'package:flutter/cupertino.dart';

const double kDefaultPadding = 12.0;
const double kDefaultBorderRadius = 6.0;

double deviceHeight(BuildContext context) => MediaQuery.of(context).size.height;
